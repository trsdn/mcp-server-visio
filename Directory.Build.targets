<Project>

  <!-- Intentionally empty.

       This project binds to Visio entirely late-bound through the "Visio.Application" /
       "Visio.InvisibleApp" ProgIDs and `dynamic` member access, so it carries no Office
       Primary Interop Assemblies and needs no office.dll copied out of the .NET Framework GAC.

       Keep this file so solution-wide build customization has an obvious home. -->

</Project>
