name: Build MCP Server

permissions:
  contents: read

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'src/VisioMcp.McpServer/**'
      - 'src/VisioMcp.Core/**'
      - 'src/VisioMcp.ComInterop/**'
      - 'tests/VisioMcp.McpServer.Tests/**'
      - 'tests/VisioMcp.Core.Tests/**'
      - 'tests/VisioMcp.ComInterop.Tests/**'
      - 'global.json'
      - 'Directory.Build.props'
      - 'Directory.Packages.props'
      - '.github/workflows/build-mcp-server.yml'
  pull_request:
    branches: [ main ]
  # Manual trigger. Also the mechanism by which a dormant workflow can be registered with, and
  # verified against, GitHub Actions - see #12.
  workflow_dispatch:

jobs:
  build-mcp-server:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 9.0.x

    - name: Restore dependencies
      run: dotnet restore VisioMcp.sln

    - name: Build solution
      # The coverage audit reads generated output from both VisioMcp.Core and VisioMcp.McpServer,
      # so the whole solution must be built before it runs.
      run: dotnet build VisioMcp.sln --no-restore --configuration Release

    - name: Verify Core Commands Coverage
      run: |
        Write-Output "🔍 Verifying Core Commands coverage..."
        & scripts/audit-core-coverage.ps1 -FailOnGaps
        if ($LASTEXITCODE -ne 0) {
          Write-Error "❌ Coverage gaps detected! All Core methods must be exposed via MCP Server."
          exit 1
        }
        Write-Output "✅ Coverage audit passed - 100% coverage maintained"
      shell: pwsh

    - name: Verify MCP Server build
      run: |
        # Check MCP Server executable
        if (Test-Path "src/VisioMcp.McpServer/bin/Release/net9.0-windows/VisioMcp.McpServer.exe") {
          Write-Output "✅ VisioMcp.McpServer.exe built successfully"
          $mcpVersion = (Get-Item "src/VisioMcp.McpServer/bin/Release/net9.0-windows/VisioMcp.McpServer.exe").VersionInfo.FileVersion
          Write-Output "📦 MCP Server Version: $mcpVersion"

          # Check for MCP server.json configuration
          if (Test-Path "src/VisioMcp.McpServer/bin/Release/net9.0-windows/.mcp/server.json") {
            Write-Output "✅ MCP server.json configuration found"
          } else {
            Write-Warning "⚠️ MCP server.json configuration not found"
          }
        } else {
          Write-Error "❌ VisioMcp.McpServer.exe not found"
          exit 1
        }

        Write-Output "🧠 MCP Server ready for AI assistant integration"
      shell: pwsh

    - name: Upload MCP Server build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: VisioMcp-MCP-Server-${{ github.sha }}
        path: src/VisioMcp.McpServer/bin/Release/net9.0-windows/
